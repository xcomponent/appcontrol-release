apiVersion: v1
kind: Service
metadata:
  name: appcontrol-frontend
  labels:
    app.kubernetes.io/name: appcontrol
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: appcontrol
    app.kubernetes.io/component: frontend
