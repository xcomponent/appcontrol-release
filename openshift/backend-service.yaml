apiVersion: v1
kind: Service
metadata:
  name: appcontrol-backend
  labels:
    app.kubernetes.io/name: appcontrol
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/name: appcontrol
    app.kubernetes.io/component: backend
