apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: appcontrol-postgres-data
  labels:
    app.kubernetes.io/name: appcontrol
    app.kubernetes.io/component: postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
